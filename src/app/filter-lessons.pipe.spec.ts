import { FilterLessonsPipe } from './filter-lessons.pipe';

describe('FilterLessonsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLessonsPipe();
    expect(pipe).toBeTruthy();
  });
});
